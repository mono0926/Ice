extends layout
block content
    link(rel='stylesheet', href='/stylesheets/todo.css')
    script(src='/javascripts/todo-app.js')

    div(ng-controller="TodoController")
        div.container
            div.header
                h1 TODO管理
            div.editor.row
                form
                    label.col-md-10
                        p やること
                        input(type="text" ng-model="text")
                    button.btn.btn-default.col-md-2(ng-click="insert()") 追加・更新
            div.todo-list.row
                div(ng-show="todoList.length == 0") ↑のフォームでTODOを入力してください
                div(ng-repeat="todo in todoList")
                    span.col-md-10(ng-class="{delete: todo.done}") {{todo.text}}
                    button.btn.btn-default.btn-sm.col-md-2(ng-click="update(todo)") {{todo.done ? '戻す' : '完了' }}
            div.cleanup.row
                button.btn.btn-warning.col-md-offset-10.col-md-2(ng-click="deleteDoneItems()") 完了済みを削除